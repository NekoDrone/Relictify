@using Relictify.Classes
@using Relictify.Shared.Components.AddChar
<div>
    @foreach(CharacterEntry Char in Chars)
    {
        <CharSelectorButton/>
    }
    <AddCharButton/>
</div>

@code {
    private int TotalChars;
    private CharacterEntry[] Chars;
}